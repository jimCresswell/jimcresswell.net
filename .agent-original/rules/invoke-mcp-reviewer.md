# Invoke MCP Reviewer

Invoke `mcp-reviewer` when changes touch `.agent/sub-agents/templates`, `.cursor`, `.claude`,
`.codex`, `.github`, or reviewer and adapter documentation. Use it for cross-platform reviewer and
adapter coherence checks.

See `.agent/sub-agents/templates/mcp-reviewer.md` for the full reviewer brief.
