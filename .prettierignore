# Ownership only. The universe is the tracked tree, computed by
# `repo-check prettier-tracked` from `git ls-files`, so build output,
# dependencies and untracked local files never reach prettier and are not
# listed here; this file declares which TRACKED surfaces prettier does not
# govern.

# Generated by the package manager
pnpm-lock.yaml

# Canonical Practice corpus and its generated platform adapters: markdownlint
# owns these surfaces; prettier never reflows them (adapters are regenerated
# by `pnpm portability:fix` and the skills adapter generator).
.agent/
.claude/skills/
.claude/rules/
.agents/
.cursor/

# Generated surfaces
RULES_INDEX.md
