#!/usr/bin/env sh

# Block accidental major-version bumps first, then enforce conventional commits.
pnpm --filter @engraph/agent-tools prevent-accidental-major-version "$1" || exit 1
pnpm exec commitlint --edit "$1"
