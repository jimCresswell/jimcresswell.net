# Invoke Config Reviewer

Invoke `config-reviewer` when changes touch build or runtime config, scripts, locks, env handling,
Next.js or PostCSS settings, or deployment-relevant tooling. Use it whenever platform behaviour
might change because configuration changed.

See `.agent/sub-agents/templates/config-reviewer.md` for the full reviewer brief.
