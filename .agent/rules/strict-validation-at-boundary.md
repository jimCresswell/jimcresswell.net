# Strict Validation At Boundary

Parse and validate external data at the boundary where it enters the system: files, network
payloads, environment values, third-party inputs, or user-supplied content. Reject malformed data
early instead of letting unchecked shapes leak inward.

See `.agent/directives/principles.md` for the full policy.
