# Invoke Reviewers

After non-trivial changes, invoke the code-reviewer sub-agent. The code-reviewer is the gateway — it assesses overall quality and triages to specialists (test-reviewer, type-reviewer, editor) as needed.

Reviewer roster: `.agent/sub-agents/templates/`
