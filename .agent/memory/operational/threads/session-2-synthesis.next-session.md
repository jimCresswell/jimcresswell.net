# Thread: session-2-synthesis — the 57 lessons and today's captures onto the register

**Thread identity.** Closure session 2 (node `.agent/plans/delivery/practice-completion.plan.md`
§Transplant closure, item 8). Owner word 2026-09-13 about 20:40Z: the Director drafts the
candidate list; lane A (Saffron turns Verdure, c39ad7) completes it as the pending-graduations
register pull request after #55 landed (the owner's amended order; #60 was not a prerequisite
and the register PR #63 merged before it), before item 5; scope is the three napkins under
`.agent/memory/active/unconsolidated/` plus today's captures in `.agent/memory/active/napkin.md`
(from line 311 to the end of the file at the commit that lands this record: sessions 4 to 8,
wraps 3 to 7, lane C, lane A, the Director's two sections); the candidates
go to the owner as one batch of cards in the morning; nothing graduates without the answers; the
napkins are archived only then.

## Participating agent identities

- Director: Cauldron herds Lustre (880ff9), claude / claude-fable-5-1, 2026-09-13.
- Implementer: Saffron turns Verdure (c39ad7), claude / claude-fable-5-1, 2026-09-13.

## Current continuation

- **Role:** lane A, Saffron turns Verdure (c39ad7), Implementer (PDR-117), completed this draft
  as the register pull request and carries its review rounds; the Director (Cauldron herds
  Lustre, 880ff9, claim `1db07581`) drafted it and routes.
- **Branch and landing target:** lane A's branch `docs/session-2-register` from `main` at
  `SHA: 7127bc4` (after #55 merged; the register PR does not wait for #60, ruled 21:55Z); the
  pull request was #63, `docs(memory): session 2 candidates onto the register`, merged by the
  bot at `SHA: dc23dff` at zero threads with Copilot bound (round one's cure `SHA: 414fb6c`);
  the branch is deleted. The live work is the owner's morning cards (§Next safe step).
- **Claim:** the register claim for the filing is closed with PR #63; each later graduation
  pull request opens its own claim on the home it edits.
- **Grounding order:** this record; the register's §What belongs here and its entry shape (the
  fitness counter reads the inline-bracket shape only); each candidate home, read first-hand.
- **Standing decisions:** the owner's 20:40Z cards (Director drafts, lane A completes; session 2
  before item 5; scope is the three napkins plus today's captures); the Director's ruling that
  the entries file in the counter's canonical shape with the five existing entries migrated,
  substance unchanged, the readout quoted as proof.
- **Acceptance:** every filed entry has a source, a candidate home read first-hand and a
  falsifiable prediction; duplicates are recorded in the PR body with the line that carries
  them; the readout moves in two proofs, 0 to 33 on filing and 33 to 28 after the five
  constitutional rows move to the slow lane (the accepted readout is 28 decision-debt blocks
  beside five slow-lane rows); the napkins are archived only after two gates, the answered
  fast-lane cards and the owner's privacy review of the three napkins.
- **Next safe step:** §Next safe step at the end of this record.

## How this draft was made

The Director (Cauldron herds Lustre, 880ff9) read every line of the four sources first-hand on
2026-09-13 between 20:45Z and 21:15Z and applied the register's admission test (learned doctrine;
a doctrine home; not yet written there). Lessons already homed by name were set aside with the
home named (§Already homed). Lessons whose home the Director could not verify without reading
it are listed for lane A to verify (§Verify before filing). Lessons that are tool traps, product
facts or plan-shaped work were routed (§Routed elsewhere). The candidate entries below follow
the register's shape. This is the consumed pre-filing draft: lane A verified each home and
filed the entries as PR #63 (`SHA: dc23dff`); the register carries the live status now.

## Candidates for the register (the consumed pre-filing draft; filed as PR #63)

### A. Ratified text is the owner's; the lenses resolve only what it leaves open

- **Source**: napkin 2026-09-13 (Wrap 7; lane A; lane C wrap): a Director verdict overturned a
  ratified todo (removal for restore) and accepted "not brought" for "create or declare"; the
  owner: "this makes me concerned for what other instructions are being overturned". Same day,
  a go on a port design contradicted testing-strategy text the seat had not re-read.
- **Doctrine**: a verdict quotes the governing text (ratified plan text, a directive) before it
  rules; it never rules against that text; a case for changing ratified text is a card to the
  owner after the item lands, never a verdict or a pull request. A verdict from memory of the
  problem is the failure shape.
- **Candidate home**: a new rule `ratified-text-changes-only-by-card` (or a clause in
  `precedence-is-not-approval`), cited from PDR-117 §Director and the Decision Lenses preamble.
- **Prediction (PDR-130)**: no Director or seat verdict contradicts a ratified line in the next
  ten sessions; every method change to ratified work arrives as a card.

### B. A method question is asked as "what is sensible under the estate's rules", never as one authority against another

- **Source**: owner, 2026-09-13 evening (about 19:10Z to 19:50Z), on the Gemini card: "this is not a matter of
  competing authorities, what does sensible look like? The Gemini adapters are generated, just
  like everything else."
- **Doctrine**: when ratified text and a later estate rule seem to disagree on method, the
  question to the owner is the sensible shape under the estate's rules, not a binary between
  two texts; the lenses' false-either/or clause applies to cards too.
- **Candidate home**: principles §Decision Lenses, one sentence after the either/or clause.
- **Prediction**: the next card that pairs "ratified text" against "an estate rule" is refused
  by its author before it is sent.

### C. Process is the last resort: a computation, an existing rule or the owner's own words answer first

- **Source**: napkin 2026-09-13 (Wrap 6, Wrap 7): four owner corrections in one afternoon with
  one shape (a rehearsal where derivation was the cure; two cards doctrine had settled; a
  precondition on optional material; a programme grown from a bounded item); the Director's
  evening repeated it (executing where routing was owed; records pushed per event).
- **Doctrine**: before adding a step, a card, a precondition, a node or a pass, ask whether a
  computation, an existing rule or the owner's recorded words already answer it; the card test
  is "could the answer be ranked from evidence or doctrine already in hand? then no card".
- **Candidate home**: `compute-dont-hope` (a clause naming the four instances) and
  `present-verdicts-not-menus` (the card test).
- **Prediction**: cards per session fall to owner-only decisions; the owner names no
  "invented optionality" in the next five card rounds.

### D. A push slot is a turn: ask, wait for the word, then push

- **Source**: napkin 2026-09-13 (lane C wrap; lane A section; Director): two seats read "tell
  me before you push" as notify-and-go; the hazard was concrete (a shared e2e port) and stays
  as host load after the port cure.
- **Doctrine**: a message naming a sequencing point is a request for a turn; the seat waits for
  the one-word confirmation; the holder releases by word. The forcing fact is named each time
  (host load; a shared port).
- **Candidate home**: `start-right-team` §5 (a slot clause) or the commit-queue reference;
  lane C's per-user memory `push-slot-is-ask-then-wait` graduates into it.
- **Prediction**: no seat pushes on its own announcement in the next ten team sessions.

### E. Proposal, owner to confirm: a state line every 120 seconds of a long turn at n=2

- **Source**: napkin 2026-09-13 (Director): lane A silent 80 minutes in a long turn; read as a
  block; escalated to the owner; the seat was fine.
- **What PDR-082 says today**: at n=2 the heartbeat cron and the 120-second message sweep are
  dropped and liveness is owner-chat responsiveness; the start-right-team overlay drops the sweep
  without sparing the progress report. So this is not existing doctrine; it is a proposal.
- **Proposal**: a seat in a long turn sends a one-line state at the team cadence, and the lead
  reads silence as unknown, never as work. If the owner declines, the lead's remedy is the
  native liveness probe (ListAgents) and a ping, never an escalation.
- **Candidate home (if confirmed)**: PDR-082 §What changes at n=2 (one sentence) and
  `start-right-team` §5.
- **Prediction (if confirmed)**: no owner escalation for a silent-but-working seat in the next
  ten sessions.

### F. Records land at waypoints, never per event

- **Source**: napkin 2026-09-13 (Director): a records pull request pushed per event drew five
  review rounds, each finding the next stale line; the cure that ended it made every
  live-state block defer to the routing log's last entry. Wrap 4 recorded the tension with the
  transplanted session-handoff clause (refreshes ride the next substantive commit).
- **Doctrine**: commit records at each state change; push once per landed merge or shape
  change; a live-state block says which line is current where it and the log disagree.
- **Candidate home**: `session-handoff` skill (resolving the Wrap 4 tension) and `pr-lifecycle`
  §Scope (a records-PR clause).
- **Prediction**: the next Director records PR settles in at most two review rounds.
  Falsified by the pull request carrying this record (#62): it carried live state through
  eighteen and more rounds, each cure's wait producing the next round's drift, because the
  branch kept receiving the night's decisions while under review (the doctrine's "push once
  per landed merge" was not practised: the push rode every event). The doctrine stands; the
  falsifier was the practice. Re-scoped prediction, filed on the register row: a Director
  records pull request opened from a branch frozen at open (later items on a stacked branch)
  settles in at most two review rounds.

### G. A cure commit adds no surface beyond the finding

- **Source**: napkin 2026-09-13 (Director, 15:15Z): a round-one cure grew a changeset by nine
  files and a reader module the next PR needed, giving the second review pass new surface; PDR-132's
  budget was exceeded.
- **Doctrine**: a cure commit is minimal; material for a later PR goes to that PR; new surface
  in a cure opens a new round by construction.
- **Candidate home**: PDR-132 (a clause) and `review-feedback-defaults-to-triage`.
- **Prediction**: no PR exceeds two rounds because a cure widened its surface, in the next ten.

### H. A record number is a foreign key without a foreign table: cite by title at the target

- **Source**: napkin 2026-09-12 (Session 4, Wrap 3): `testing-strategy.md` cited three lineage
  ADRs whose numbers matched local records with different subjects; the link repair was one
  referential-integrity pass, not 418 edits.
- **Doctrine**: a citation by record number is verified by the record's title at the target
  before it is kept; a transplant runs that check as one pass.
- **Candidate home**: `documentation-hygiene` (a clause) and PDR-005 §Scrub (the transplant
  form); the cited-paths validator is its mechanism for paths, not for numbers.
- **Prediction**: the next transplant's number-citation scrub finds zero mismatches after the
  pass, and the first cited-record validator (numbers to titles) lands as a docs leg.

### I. Verify an explorer's violation claim against the validator that polices it before repeating it

- **Source**: napkin 2026-09-12 (Session 4): a sub-agent's "violation" claim reached a peer
  seat before the reference-direction validator's own count (zero) was read; retracted.
- **Doctrine**: a claimed violation of a policed rule is checked against the policing
  validator's output first; the validator's verdict outranks any reader's.
- **Candidate home**: `verify-dont-trust` (a clause) and `read-diagnostic-artefacts-in-full`.
- **Prediction**: no finding is sent to a peer or the owner that its validator contradicts.

### J. Check that a mechanism existed at time T before hypothesising why it failed at T

- **Source**: napkin 2026-09-12 (Session 5): a "missed startup write" diagnosis and a timeout
  change cured a non-event; the hook did not exist at the startup in question (one
  `git log --diff-filter=A` settles it).
- **Doctrine**: a failure hypothesis names the mechanism's existence at the time as its first
  premise; the check is one command.
- **Candidate home**: `verify-dont-trust` (a clause) or a `patterns/` file
  (`mechanism-existed-at-time-t`).
- **Prediction**: no structural change lands on a misdiagnosis of this shape in the next ten
  sessions.

### K. Converge on the lineage's live practice; never alias two conventions after a transplant

- **Source**: napkin 2026-09-12/13 (Session 5, Wrap 4): two gate-naming conventions kept side
  by side and aliased until the owner ruled "adopt OCE naming"; the retrospective named the
  shape "add where convergence is wanted".
- **Doctrine**: when a transplant leaves two conventions live, converge on the lineage's
  practised one and surface the choice; an alias layer is a compatibility layer.
- **Candidate home**: `replace-dont-bridge` (a transplant clause) and PDR-005.
- **Prediction**: the next transplant leaves no aliased convention at its first gate-green.

### L. Antigen density chooses the merge method; a host template's paths are assertions to exercise

- **Source**: napkin 2026-09-12 (Session 5, slice 2 item 2): under five antigen lines, copy and
  graft; over fifteen, rewrite on the lineage's structure; two host paths were wrong and found
  only by `ls`.
- **Doctrine**: the transplant's file-merge method is chosen by measured antigen density, and
  every path a merged surface names is exercised before the merge lands.
- **Candidate home**: PDR-005 (method section); the runbook node cites it.
- **Prediction**: the runbook's step 6 names the thresholds and the next transplant records its
  densities.

### M. Completeness is judged by function and exercised claims, never by a green gate; a ruling to trim is scoped to the default it was made under

- **Source**: napkin 2026-09-13 (Wrap 5; Session 6): the estate called itself complete when
  `check` was green while doctrine cited forty absent paths; the owner flipped the default
  ("bring unless product") and earlier trims reopened.
- **Doctrine**: a transplant's completeness proof is the function-by-function audit with
  exercised claims (the cited-paths check as the cheapest instrument); every trim ruling
  records the default it was made under and reopens when the default changes.
- **Candidate home**: PDR-005 §Completeness and `practice-verification`.
- **Prediction**: the next transplant's completion report cites nine function rows with proofs
  before any "complete" word, and its trim rulings name their default.

### N. A projection generator preserves role multiplicity; the census against the pin proves it

- **Source**: napkin 2026-09-13 (Director): the one-wrapper-per-template projection flattened
  the four pinned Cricket roles into two generic wrappers; a computed census against the pin
  found it and three stubbed rules.
- **Doctrine**: adapter generation preserves many-to-one projections (roles per template with
  their pins); a transplant runs a projection census (counts and pinned sets per surface class)
  against the pin as an audit step.
- **Candidate home**: PDR-009 (generator direction) and the runbook's completeness step.
- **Prediction**: the sub-agent adapter generator (item 6, 2b) emits each platform's pinned
  Cricket role set from a role declaration (Claude and Cursor four roles, Codex three; a
  three-role Codex projection is correct, not a failure), and the census is a leg or an audit
  script under `inputs/`.

### O. Manifest declarations: runtime-created is declared; superseded is not declared at all

- **Source**: napkin 2026-09-13 (lane C): a surface absent until its first writer acts is
  declared runtime-created (the commit-queue shape); a surface doctrine has superseded gets no
  entry, because "must never exist" is a memorial.
- **Doctrine**: the substrate manifest declares presence and lifecycle; absence-by-supersession
  lives in the superseding record, never in the manifest.
- **Candidate home**: the substrate manifest's README and `no-tombstones-for-removed-ideas`.
- **Prediction**: no manifest entry of the form "never exists" is authored in the next ten
  sessions.

### Q. Split proofs by layer: contracts in Vitest, emitted channels in Playwright; E2E sources graph-backed expectations from raw JSON fixtures

- **Source**: napkin 2026-03-09 (Track A A3 slices, three instances): importing a product module
  into a Playwright spec failed on bundler-resolved JSON imports; the clean split kept the
  contract assertion in Vitest and the emitted-channel assertion in Playwright, with
  `content/entities.json` imported with a JSON import attribute on the E2E side.
- **Candidate home**: `testing-strategy.md` §Site Workspace Conventions (one bullet) and
  `docs/engineering/testing-patterns.md`.
- **Prediction**: no site E2E spec imports an app module in the next ten sessions.

### R. Prove a checker with a negative control

- **Source**: napkin 2026-03-08 (harness proofs: an isolated temp repository from `git archive`
  plus a deliberate visual change; the red phase of the schema-dts guard proved by adding the
  historical failure and watching typecheck fail); the register's own admission example names
  this pattern as unhomed.
- **Candidate home**: `patterns/prove-the-checker-with-a-negative-control.md`; the
  validation-strategy §Prove the guard bites clause cites it as the checker-level form.
- **Prediction**: the pattern file exists and is cited from validation-strategy; the next new
  validator's PR names its negative control.

### S. Stable current-state architecture truth moves out of plans and audits into permanent architecture docs

- **Source**: napkin 2026-03-09 (Permanent Graph Truth): the split-ownership truth (page JSON
  renders; the graph drives JSON-LD and the manifest) lived only in an audit until moved to
  `docs/architecture/`; historical ADRs stay accepted but carry a clarification note when the
  architecture moves on.
- **Candidate home**: `documentation-hygiene` (a clause) and the docs-adr-expert template
  (a check row).
- **Prediction**: no current-state architecture fact lives only in a plan or audit at the next
  consolidation.

### T. When a source-of-truth boundary lands, search historical accepted ADRs and current plans for the superseded field names

- **Source**: napkin 2026-08-12: older accepted ADRs and a current plan still spoke of
  `meta.summary` and live tilt routes after the boundary landed.
- **Candidate home**: `documentation-hygiene` (the same clause as S, second instance) and the
  docs-adr-expert template.
- **Prediction**: the next boundary change's PR lists the grep of superseded names it ran.

### V. CLI file writers validate names at the boundary, write atomically and never follow links

- **Source**: PR #55's fourth and fifth rounds (2026-09-13): a rule-name path escape, a
  truncating write that broke resumability, a symlink write-through; each cured at the
  boundary with a Result refusal, an atomic writer and an lstat seam.
- **Candidate home**: a `patterns/` file (`cli-writer-boundary-discipline`) cited by the
  security-expert and code-expert templates.
- **Prediction**: the next CLI that writes files under a caller-supplied name ships with these
  three cells in its first PR.

### W. Hook substring policy reads prose too: records prose that names git operations is written through the file tool

- **Source**: napkin 2026-09-13 (Director; lane C): a commit message's "restore", a log
  entry's "checkout", a heredoc's "git", a `-f` flag on an unrelated call in the same command.
- **Candidate home**: `hook-policy-substring-discipline` (a worked-shapes clause).
- **Prediction**: no seat reports a hook refusal on prose in the next ten sessions.

### X. An ARC channel file committed on one branch is deleted by a branch switch in the primary working copy

- **Source**: napkin 2026-09-13 (Director): the channel file, committed on the records branch,
  vanished from disk on a switch to a branch cut from `main` and reappeared on the switch back;
  the tail replayed the file; a partner append in the window would have blocked the switch.
- **Doctrine**: while a channel is open, the primary working copy stays on the branch that
  carries the channel file; other branches' work runs in worktrees.
- **Candidate home**: `.agent/reference/arc-rapid-communication.md` §Operating constraints.
- **Prediction**: no channel tail replays a file in the next ten team sessions.

## Verify before filing (lane A reads the home; files as `duplicate` where it already carries the lesson)

- `verify-data-supports-shape-before-building`: whether its text covers the lane C instance (a
  `curator-passes/` README imported because a report listed it as a register the loop writes to,
  while PDR-081 and the curator-pass skill say the opposite: a directory the lineage kept for
  history is not a register the doctrine writes to). If not, a worked-instance clause.

- The consolidate-docs skill: one truth-maintenance pass over frontmatter status, narrative
  status, next-step sections, current-state notes, roadmap and parent tables, and READMEs that
  advertise plan state (napkin 2026-03-09 and 2026-04-03 say promoted; confirm the transplanted
  skill kept it). Also: consolidation checks a domain skill's pitfalls table against findings
  since the skill was written (2026-03-07).
- The plan skill: a plan is complete only with verification evidence recorded ("code complete"
  is not complete; gates are acceptance criteria; 2026-03-06); a platform-integration plan that
  settles reusable architecture is mined into an ADR before closing (2026-03-08); losing paths
  of a design family are executable and a dated FAIL is a disposition (2026-08-12).
- `testing-strategy.md`: large fixtures, allowlists or helper definitions inside a test are a
  design smell that moves logic to product code (owner preference 2026-03-08).
- `.agent/reference/tooling.md` and `typescript-gotchas.md`: `pnpm up --latest` moving eslint
  past Next's range; Playwright browsers reinstalled after an `@playwright/test` update; flat
  ESLint config does not encode local policy without explicit rules; markdownlint needs explicit
  globs; knip flags CLI-loaded plugins (an `ignoreDependencies` entry with its reason).
- ADR-022 and the harness README: compare refs by `git archive` into temp dirs; structural
  selectors, never refactor-introduced ids; record the artefact directory of the first failing
  run; one-way auto-acceptance of expected anchors; a DOM-only difference is a review question;
  re-run after any late page-output fix; a harness-only allowlist is the wrong abstraction when
  HTML is data (2026-03-08, six lessons).
- `complex-merge` and `semantic-merge` skills, `pre-merge-divergence-analysis`: stacked PRs with
  overlapping continuity files have no safe unchanged order; rebuild the bounded story on fresh
  `main` after custody is proved (2026-08-12).
- `reference/merge-bot.md` or a git reference: local `git log --show-signature` needs an
  allowed-signers file to display what GitHub verifies (2026-08-12).
- `validation-strategy.md` §Gate integrity: a new CI leg's first green must be a CI run, not the
  host (lane A, 2026-09-13).

## Already homed (set aside with the home)

- Never run `check` and the e2e suite in parallel: principles §Quality gates (lane A trues the
  rationale in PR #60).
- Permanent docs never reference ephemeral plans: principles §Documentation Is Infrastructure.
- Rename everywhere; grep all instances of a stale pattern: principles §Code Design, §Refactoring.
- Runtime constants `as const` as the type source: validation-strategy.
- Graduate a durable preference immediately with an anchor; the buffer pipeline:
  `per-user-memory-is-a-buffer`, `napkin-always-active`.
- Automated reviewers do not understand the portability architecture; triage independently:
  `review-feedback-defaults-to-triage`.
- Tighten before adding near a ceiling; never trim substance to a limit:
  `knowledge-preservation-over-fitness-warnings`.
- Clean breaks over stubs for planning artefacts: `replace-dont-bridge`.
- Production build is the e2e layer; dev-server transients removed at source: ADR-019.
- Zero pixel difference can still carry semantic HTML changes: testing-strategy §Site conventions.
- Provenance ids are UUIDs; chronology in chain order and date: `practice-core/provenance.yml`.
- Green locally, red in CI is local state the checkout lacks; compute the list from its source:
  `compute-dont-hope`, validation-strategy §Gate integrity.
- The generalisation register is a declaration of intent: `record-generalisation-moves`.
- A permanent page names the ruling and date, never the vehicle: `no-moving-targets-in-permanent-docs`.
- Read the writer before building the surface: `verify-data-supports-shape-before-building` (the
  verify list carries the lane C instance check).
- The channel by audience, lifetime and consumer: `channel-by-audience-lifetime-and-consumer`.
- The dual-scale Cricket labels: the cricket skill.
- Re-evaluate is an assertion-exercise pass: register entry 5.
- Owner direction is settled once given; proof is always required: principles §Owner Direction
  Beats Plan, `verify-dont-trust`.

## Routed elsewhere (not graduations)

- Tool traps (distilled §Troubleshooting or the frictions register): `pnpm -s`; zsh `path`;
  `$P:agent-tools` modifier parsing; brackets in route paths; `${PIPESTATUS}` in zsh; perl
  delimiters and `${…}` interpolation; temp-file loops that write on failure; number-to-noun
  substitution needing an article pass; `comms send --body` long inline writing nothing;
  commitlint footer tokens and the 100-character header; `erasableSyntaxOnly` and constructor
  properties; `Array.isArray` on `readonly string[]`; MD028 after a provenance blockquote; the
  worktree guard's refusals (git in heredoc text, tilde paths, piped git); a Copilot request
  while the previous review run is live registers nothing; the merge bot's GraphQL logins.
- Product facts (the PKG skill, ADR-014, the Track A proof records, editorial-guidance): the
  Schema.org pitfalls, the schema-dts hybrid guard, the graph publication contracts, external
  validator behaviour, the editorial facts of 2026-02-20.
- Doc truings, not graduations: ADR-015 says Codex has no `.agents/rules/` layer; the estate
  now projects 130 `.agents` rules (surface matrix and ADR-015 to true up; a small follow-on).
- Plan-shaped: the transplant runbook steps that L, M and N cite (the doctrine goes to PDR-005
  and PDR-009; the runbook cites them).
- Owner review in the morning (privacy): an owner-gated privacy review of the three napkins and
  of any public plan under `privacy.md`, before the napkins are archived; the synthesis PR
  carries nothing from them beyond the candidates' doctrine. (Candidate U was withdrawn into
  this line: a review requirement is a process step, not a graduation.)

## Next safe step (after PR #63 merged at `SHA: dc23dff`, 2026-09-13 22:39Z)

The batch is filed and on `main`: twenty-eight session 2 entries in the counter's shape, the
five earlier entries migrated, five constitutional-class entries in PDR-130's slow lane with
review dates, the readout at 28 pending. Nothing here refiles or reclassifies the register.

1. The owner's morning cards (the ratified item 8): the Director presents the filed entries in
   the four classes of the PR body, plus proposal E. For the twenty-three fast-lane entries each
   answer is the fast disposition (`graduated`, `rejected`, `duplicate`), recorded on the entry.
   For the five slow-lane entries (A, B, C, 1a, 1b) the card is PDR-130's review-date decision,
   `promote` or `kill-with-reasoning`, recorded on the slow-lane row; a card left unanswered
   leaves the row for its review date (2026-12-13).
2. Every fast-lane outcome drains its row (drain by deciding, the register's contract). A
   `graduated` entry lands as its own small pull request into the named home, one home per PR
   where the homes differ, by the seat the Director routes, and the entry is removed in the
   same PR. A `rejected` entry is removed in a register commit whose drain note (the register's
   HTML-comment drain record) carries the rejection reason. A `duplicate` entry is removed the
   same way, the drain note naming the home that carries it. No decision leaves the durable
   record: the commits, the homes and the drain notes are the record.
3. The three napkins under `.agent/memory/active/unconsolidated/` are archived once both
   gates hold: the fast-lane cards of step 1 are answered, and the owner's privacy review of
   the three napkins under `privacy.md` (overnight item 5) is done. The slow-lane cards are
   not an archive gate: an unanswered slow-lane card holds only its own row until the review
   date, because the row conserves its source text (the archive holds processed material only,
   and filing is the processing).
4. The slow-lane five are decided at their review date (the owner's card, else 2026-12-13).
